{
  "name": "mediant",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "mediant": "./server/cli.mjs"
  },
  "files": [
    "dist",
    "server"
  ],
  "scripts": {
    "build": "vite build",
    "start": "vite build && node server/cli.mjs",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "vite": "^7.3.2",
    "vitest": "^3.1.0"
  }
}
